# 核心依赖
requests>=2.25.0          # HTTP请求库，用于调用IP地理位置API

# 开发和调试工具
pytest>=7.0.0            # 单元测试框架
black>=22.0.0            # 代码格式化工具
flake8>=4.0.0            # 代码风格检查
isort>=5.10.0            # 导入语句排序

# 性能和监控
psutil>=5.8.0            # 系统和进程监控
rich>=12.0.0             # 终端输出美化
 tqdm>=4.64.0            # 进度条显示

# 网络和并发
aiohttp>=3.8.0           # 异步HTTP客户端（可选，用于未来异步版本）
asyncio-throttle>=1.0.0  # 异步限流器

# 数据处理
pandas>=1.4.0            # 数据处理和分析（可选，用于结果分析）